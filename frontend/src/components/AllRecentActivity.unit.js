import AllRecentActivity from './AllRecentActivity'

describe('@components/AllRecentActivity', () => {
  it('exports a valid component', () => {
    expect(AllRecentActivity).toBeAComponent()
  })
})
